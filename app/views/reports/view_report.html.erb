<h2>Report: <%= @report_title %></h2>

<table id="report-results">
  <thead>
    <tr>
      <th>Title</th>
      <th>Loan Count</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |b| %>
      <tr>
        <td><%= link_to b.title, book_path(b) %></td>
        <td><%= b.loans.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>