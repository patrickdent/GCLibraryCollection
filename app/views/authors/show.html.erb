<h1><%= @author.name %></h1>

<table class="index">
  <tr>
    <th>Title</th>
    <th>Genre</th>
  </tr>

  <% @author.books.each do |b| %>
    <tr>
    <td><%= link_to b.title, book_path(b.id) %></td>
    <td><%= b.genre.name if b.genre %></td>
    </tr>
  <% end %>
</table>