<%= form_for @loan do |l| %>

  <%= l.hidden_field :book_id %>

  <%= l.label :user_id %>
  <%= l.select :user_id, User.all.collect { |u| [u.name, u.id] }, {}, multiple: false %>

  <%= l.submit "submit" %>

<% end %>