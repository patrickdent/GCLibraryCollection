<%= form_for @loan do |l| %>

  <%= l.hidden_field :book_id %>

  <%= l.label :user_id %>
  <%= l.select :user_id, User.able_to_borrow.collect { |u| [u.name, u.id] }, {}, multiple: false, class: "chosen-select" %>

  <%= l.submit "submit" %>

<% end %>