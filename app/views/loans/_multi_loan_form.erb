<% if true %>
  <%= form_tag(loan_multi_path, method: "post") do %>
    <% params[:books].each do |b| %>
      <%= hidden_field_tag("book_ids[]", b) %>
    <% end %>
    <%= select_tag(:user_id, options_from_collection_for_select(User.all, :id, :name), multiple: false, class: "chosen-select") %>
    <%= submit_tag("submit") %>
  <% end %>
<% end %>