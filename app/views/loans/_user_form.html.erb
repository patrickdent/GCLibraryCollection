<%= form_for @loan do |l| %>

  <%= l.hidden_field :user_id %>

  <%= l.label :book_id %>
  <%= l.select :book_id, Book.all.collect { |b| [b.title, b.id] }, {}, multiple: false %>

  <%= l.submit "submit" %>

<% end %>